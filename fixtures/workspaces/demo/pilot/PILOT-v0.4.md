# Pilot metrics sample (scores PASS)

## Scores (machine) — required

```
baseline_miss_spec=4
pilot_miss_spec=1
baseline_reopen=4
pilot_reopen=1
baseline_escape=2
pilot_escape=0
gate_blocks=3
tickets_completed=10
```
