# Coaching ticket <NNN>: [short title]

- **Status:** OPEN
- **Opened:** [YYYY-MM-DD] by :learning ([repo]/[file:line or area])
- **Closed:** — (filled on close)
- **Target:** domain-knowledge/[domains/X.md or business.md] (where the answer will land)

## Thread

### Q1 ([YYYY-MM-DD], :learning)
[AI's question — cite the specific code it couldn't interpret, e.g. "app/services/
job_matcher.rb:40 has a `boost_score` multiplier applied only when
`company.plan == 'enterprise'`. What business rule does this implement — is this a
paid visibility boost, or something else?"]

### A1 ([YYYY-MM-DD], user)
[user's raw answer, verbatim]

<!-- Repeat Q<n>/A<n> as needed. Add a Q<n> only when the previous answer still
     leaves the business purpose unclear after re-investigating code with that
     answer as a lead — do not ask a follow-up just to double-check an already
     clear answer. -->

## Resolution (filled at CLOSED)

- **Understood:** [the fact, in the same concrete style as domains/*.md — see
  references/learning.md's "captured enough" bar: name the rule, the entities
  involved, and cite the code path, not a vague restatement]
- **Written to:** [file:section]
- **Conflict check:** [none found — searched: <file(s)/section(s) actually checked, not just
  Target> | found — see coaching diff in changelog.md row. A bare "none found" with no searched-
  files list does not count as a completed check — see skills/coaching/SKILL.md.]
